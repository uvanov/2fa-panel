import styled from '@emotion/styled';
import {
  Flex,
  Grid
} from '../../../../ui/ui';

export const StyledReserve = styled(Flex)`
  padding: 0 20px;
`;

export const ReserveCodeGrid = styled(Grid)`
  width: 80%;
  margin: 0 auto;
`;
