import styled from '@emotion/styled';
import {
  Grid,
  Typography
} from '../../../../ui/ui';

export const ReserveCodeGrid = styled(Grid)`
  width: 80%;
  margin: 0 auto;
`

export const ReserveCode = styled(Typography)`
  width: fit-content;
  font-family: Consolas;
  font-weight: 800;
`;